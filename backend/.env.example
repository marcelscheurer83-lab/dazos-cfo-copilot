# Optional: add your OpenAI or other AI API key for Copilot answers
# COPILOT_API_KEY=
DATABASE_URL=sqlite+aiosqlite:///./cfo.db

# Phase 1a: Google Sheets (financial model/plan)
# Get the spreadsheet ID from the sheet URL: https://docs.google.com/spreadsheets/d/<SPREADSHEET_ID>/edit
# GOOGLE_SHEET_ID=
# Path to the service account JSON key file (create in Google Cloud Console, enable Sheets API, share the sheet with the service account email)
# GOOGLE_APPLICATION_CREDENTIALS=./credentials/google-service-account.json
# Or paste the full JSON key as a single line (e.g. in a secret manager):
# GOOGLE_SHEETS_CREDENTIALS_JSON={"type":"service_account",...}
# For ARR export to Sheet: range in A1 notation (default ARR!A1:Z200). Create a sheet tab named "ARR" and share the sheet with the service account.
# GOOGLE_SHEET_ARR_EXPORT_RANGE=ARR!A1:Z200
# Optional: create a NEW sheet in a user's Drive each export (no overwrite). Set to the user's email (e.g. marcel@dazos.com).
# Requires Google Workspace Admin: Domain-wide delegation for the service account with scopes drive.file and spreadsheets.
# GOOGLE_EXPORT_AS_USER=marcel@dazos.com

# Phase 1b: Salesforce (ARR + GTM)
# Get security token from Salesforce: Profile → Settings → Reset My Security Token (emailed to you)
# SALESFORCE_USERNAME=your.email@company.com
# SALESFORCE_PASSWORD=your_password
# SALESFORCE_SECURITY_TOKEN=your_token
# SALESFORCE_DOMAIN=login   # use 'test' for sandbox

# Phase 1c: QuickBooks Online (P&L, Balance Sheet, Cash Flow)
# Create an app at https://developer.intuit.com; complete OAuth once to get a refresh token.
# QB_CLIENT_ID=your_client_id
# QB_CLIENT_SECRET=your_client_secret
# QB_REALM_ID=your_company_id   # from QuickBooks URL or API
# QB_REFRESH_TOKEN=your_refresh_token
# QB_SANDBOX=true   # set to true if using Sandbox/Development company (avoids 403 on reports)

# When publishing the app: comma-separated origins allowed by CORS (e.g. https://your-app.vercel.app)
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://your-frontend-url.com

# Optional: require a shared password to access the app and API (set on backend; users enter it on the login screen).
# APP_PASSWORD=your-secret-password
